{
  "name": "demon-bot",
  "version": "1.5.0",
  "description": "Discord bot with quote images",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "git add .; if (-not (git diff --cached --quiet)) { git commit -m \"Update quote bot - spacing & emoji fix - $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')\"; git push origin main } else { Write-Host 'No changes to commit' }"
  },
  "dependencies": {
    "canvas": "^3.2.3",
    "discord.js": "^14.26.0",
    "dotenv": "^16.6.1",
    "node-fetch": "^2.7.0",
    "sharp": "^0.34.5"
  }
}